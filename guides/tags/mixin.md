# @mixin

### Syntax

`@mixin [<name>]`

### Overview

This tag declares a "mixin" − a partial class that can be [mixed in](./mixes.html) to another class.

### Example(s)

```js
/**
 * @mixin
 */
const MixinA = {
  /**
   * @method
   */
  doIt() {
    
  }
}
```

```js
/**
 * @mixin
 */
class MixinB {
  
}
```
